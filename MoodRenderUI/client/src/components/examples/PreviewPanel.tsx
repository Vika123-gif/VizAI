import PreviewPanel from '../PreviewPanel';

export default function PreviewPanelExample() {
  return (
    <div className="p-8 bg-background min-h-screen">
      <PreviewPanel 
        moodboardPreview={null} 
        renderPreview={null}
      />
    </div>
  );
}